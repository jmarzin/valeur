= form_for @strategy do |f|
  - if @strategy.errors.any?
    #error_explanation
      %h2= "#{pluralize(@strategy.errors.count, "error")} prohibited this strategy from being saved:"
      %ul
        - @strategy.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
